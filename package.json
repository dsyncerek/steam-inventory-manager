{
  "name": "steam-inventory-manager",
  "version": "1.0.0",
  "scripts": {
    "start:dev": "lerna run --parallel start:dev",
    "start:prod": "lerna run --stream start:prod",
    "build": "lerna run --stream build",
    "format": "lerna run --stream format",
    "lint": "lerna run --stream lint",
    "postinstall": "lerna bootstrap"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run precommit"
    }
  },
  "devDependencies": {
    "husky": "^3.0.5",
    "lerna": "^3.16.4"
  }
}
